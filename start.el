(load-file "Zettel.el")
(load-file "org-mode.el")
(load-file "characters.el")

(global-visual-line-mode)
(org-mode)

(defun ID0 ()
   "This is a way of exiting the M-x line."
   (interactive)
   (message "logicinaction.org/docs/ch6.pdf ε")
   )
; (describe-function 'ID0)

(setq phi 1.6180339887)
